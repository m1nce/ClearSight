name: clearsight
channels:
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - pip
  - python-dotenv
  - requests
  - albumentations
  - numpy
  - opencv
  - tqdm
  - nb_conda_kernels
  - jupyter
  - matplotlib
  - pip:
      - gdown
      - torch
      - torchvision
      - ipykernel
prefix: /opt/anaconda3/envs/clearsight
