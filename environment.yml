name: ClearSight
channels:
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - pip
  - python-dotenv
  - requests
  - albumentations
  - numpy
  - opencv
  - tqdm
prefix: /opt/anaconda3/envs/ClearSight
