name: clearsight
channels:
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - pip
  - python-dotenv
  - requests
  - numpy
  - opencv
  - tqdm
  - nb_conda_kernels
  - jupyter
  - matplotlib
  - pip:
      - gdown
      - torch
      - torchvision
      - ipykernel
      - albumentations
      - ultralytics
prefix: /opt/anaconda3/envs/clearsight
